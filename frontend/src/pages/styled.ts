import { styled } from '@mui/material';

export const ErrorStatus = styled('div')(({ theme }) => ({
  maxWidth: '80rem',
  marginTop: '10rem',

  textAlign: 'center',
  //   fontFamily: theme.typography.body.h4.fontFamily,
  //   fontSize: theme.typography.body.h4.fontSize,
}));
