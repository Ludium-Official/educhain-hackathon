'use client';

import React from 'react';
import Image from 'next/image';
import { ContentContainer } from './common/ContentContainer';

export const AddMissions = () => {
  return (
    <ContentContainer contentHeader="Missions">
      <div></div>
    </ContentContainer>
  );
};
